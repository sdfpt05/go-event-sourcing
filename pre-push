#!/bin/sh

# Run Go tests
echo "Running tests..."
go test ./...

# Capture the exit code
TEST_EXIT_CODE=$?

# If the tests failed, prevent the push
if [ $TEST_EXIT_CODE -ne 0 ]; then
    echo "Tests failed. Push aborted."
    exit 1
fi

# If tests passed, allow the push
exit 0